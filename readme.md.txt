银行ATM 取款机模拟系统 
1、运用指针、结构体及文件相关操作，对不同模块进行了有效封装； 
2、实现用户的存取款、余额查询、挂失及管理员对账户的冻结、注销等操作
 
该项目比较简单，但也有亮点，其中在用户操作中添加了用户验证模块，
当用户输错密码超过3次时，即被冻结，此时设置标志位flag=1。
当再次进行用户验证时，即可由此标志位区分出用户存在但同时被冻结的情况。
这就需要在用户模块和管理员模块进行很好的封装操作。